{
  "name": "loopbench-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "An eval + adversarial-robustness layer for agent loops: stress-test the routing/tool-selection decision and export failures as post-training signal.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "loopbench": "pnpm --filter @loopbench/cli exec loopbench"
  },
  "keywords": [
    "ai-agents",
    "agent-harness",
    "loop-engineering",
    "tool-routing",
    "eval",
    "red-team",
    "robustness",
    "mcp"
  ]
}
